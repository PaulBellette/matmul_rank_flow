[project]
name = "matmul-rank-flow"
version = "0.1.0"
description = "Tiny continuous-relaxation experiments for matrix multiplication tensor rank"
requires-python = ">=3.11"
dependencies = [
    "torch>=2.10",
    "numpy>=1.26",
    "mpmath>=1.3",
    "scipy>=1.11",
    "sympy>=1.12",
]

[tool.uv]
package = false
